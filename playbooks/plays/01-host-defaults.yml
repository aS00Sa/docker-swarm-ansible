---
# ansible-playbook -i inventory-prod.ini playbooks/plays/01-host-defaults.yml
- hosts: all
  tags: [host-defaults]
  roles:
    - role: ../roles/cluster-defaults
  tasks:
    - import_role:
        name: ../roles/host-defaults
      tags: [host-defaults]
