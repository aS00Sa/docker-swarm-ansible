---
# ansible-playbook -i inventory-prod.ini playbooks/plays/09-traefik.yml
- hosts: traefik
  tags: [traefik]
  roles:
    - role: ../roles/cluster-defaults
  tasks:
    - import_role:
        name: ../roles/traefik
      tags: [traefik]
