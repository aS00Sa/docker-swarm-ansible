---
# ansible-playbook -i inventory-prod.ini playbooks/plays/09-swarm-labels.yml
- hosts: swarm_nodes
  tags: [swarm-labels]
  roles:
    - role: ../roles/cluster-defaults
  tasks:
    - import_role:
        name: ../roles/swarm-labels
      tags: [swarm-labels]
