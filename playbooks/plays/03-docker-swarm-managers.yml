---
# ansible-playbook -i inventory-prod.ini playbooks/plays/03-docker-swarm-managers.yml
- hosts: swarm_managers
  tags: [docker, swarm-managers]
  roles:
    - role: ../roles/cluster-defaults
  tasks:
    - import_role:
        name: ../roles/docker
      tags: [docker]
    - import_role:
        name: ../roles/swarm-managers
      tags: [swarm-managers]
