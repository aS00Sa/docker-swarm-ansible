---
# ansible-playbook -i inventory-prod.ini playbooks/plays/02-generate-certs.yml
- hosts: all
  tags: [generate-certs]
  roles:
    - role: ../roles/cluster-defaults
  tasks:
    - import_role:
        name: ../roles/generate-certs
      tags: [generate-certs]
