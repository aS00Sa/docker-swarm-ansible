---
# Локальные каталоги (НЕ GFS) для inhome-infra, на swarm_nodes.
# Бэкапы Postgres: bind на хосте под корнем /backups (не /var/backups/...).
local_backup_paths:
  - path: /backups/inhome-infra/postgres-backups
    mode: "0750"

