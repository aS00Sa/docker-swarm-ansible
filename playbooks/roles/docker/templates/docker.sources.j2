# Managed by Ansible — Docker Inc. apt repo (как в документации Docker; без Architectures — иначе apt может не видеть пакеты)
Types: deb
URIs: https://download.docker.com/linux/{{ ansible_distribution | lower }}
Suites: {{ ansible_distribution_release }}
Components: stable
Signed-By: /etc/apt/keyrings/docker.asc
